server:
  port: 8953

spring:
  application:
    name: tonghua-system
  profiles:
    active: dev

mybatis:
  mapper-locations: classpath:mappers/*.xml
  configuration:
    map-underscore-to-camel-case: true
